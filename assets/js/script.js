const swiper = new Swiper('.swiper', {
    loop: true,
    pagination: {
        el: '.swiper-pagination',
    },

    scrollbar: {
        el: '.swiper-scrollbar',
    },
    // autoplay: {
    //     delay: 2500,
    //     disableOnInteraction: false,
    // },
});